{
  "name": "gw2w2w-cloudflare",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo dev --parallel",
    "preinstall": "npx only-allow pnpm",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.7.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.4"
}
